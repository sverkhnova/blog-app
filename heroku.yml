setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  command:
    - npx prisma migrate deploy
  config:
    DATABASE_URL: your_database_url
run:
  web: npm start
